<div class="container">

  <h1>Cocktails</h1>

   <ul>
    <% @cocktails.each do |element| %>
     <li>
       <%= link_to element.name.capitalize, cocktail_path(element) %>
     </li>
    <% end %>
   </ul>

  <%= link_to "New Cocktail", new_cocktail_path, class: "btn btn-primary"%>
</div>
